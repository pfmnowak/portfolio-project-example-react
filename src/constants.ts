export const API_URL = 'https://62cbcfcd8042b16aa7c2d987.mockapi.io/blog/api/';
export const colorPrimary = '#00bcd4';
export const ITEMS_PER_PAGE = 4;
export const TOTAL_COUNT = 89; // For server-side pagination we need to set this manually since the API doesn't return this value. Or we can also send 2 requests, but then the client-side pagination would be better
